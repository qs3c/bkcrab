# 工具参考

## 内置工具

### exec
执行 Shell 命令，带超时和安全检查。
- 危险命令（rm -rf /、mkfs 等）会被阻止
- 默认超时：30 秒

### read_file
读取文件内容。支持绝对路径或相对于工作区的路径。

### write_file
将内容写入文件。按需创建父目录。

### list_dir
列出文件和目录，包含大小信息。

### message
向指定渠道和聊天 ID 发送消息。