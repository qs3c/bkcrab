# 可选的集群内 MySQL，适用于开发和单节点试验。
# 生产环境建议使用托管的 MySQL 8.0+ 服务。
#
# 与 fastclaw.yaml 一起使用：
#   1. kubectl apply -f mysql.yml
#   2. 设置 bkcrab-secrets.STORAGE_DSN 为：
#        bkcrab:<MYSQL_PASSWORD>@tcp(bkcrab-db:3306)/bkcrab?parseTime=true&loc=UTC&charset=utf8mb4
#   3. kubectl apply -f fastclaw.yaml
apiVersion: v1
kind: Secret
metadata:
  name: bkcrab-db
  namespace: bkcrab
type: Opaque
stringData:
  MYSQL_PASSWORD: "change-me-in-production"
  MYSQL_ROOT_PASSWORD: "change-root-password-too"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bkcrab-db
  namespace: bkcrab
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bkcrab-db
  namespace: bkcrab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bkcrab-db
  template:
    metadata:
      labels:
        app: bkcrab-db
    spec:
      containers:
      - name: mysql
        image: mysql:8.4
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_DATABASE
          value: bkcrab
        - name: MYSQL_USER
          value: bkcrab
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: bkcrab-db
              key: MYSQL_PASSWORD
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: bkcrab-db
              key: MYSQL_ROOT_PASSWORD
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
        readinessProbe:
          exec:
            command:
              - sh
              - -c
              - mysqladmin ping -h 127.0.0.1 -u bkcrab -p"$MYSQL_PASSWORD" --silent
          initialDelaySeconds: 10
          periodSeconds: 5
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: bkcrab-db
---
apiVersion: v1
kind: Service
metadata:
  name: bkcrab-db
  namespace: bkcrab
spec:
  selector:
    app: bkcrab-db
  ports:
  - port: 3306
