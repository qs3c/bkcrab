# 可选 — 集群内自托管 Postgres，适用于开发/单节点试验。
# 生产环境建议使用托管服务（阿里云 RDS / AWS RDS 等），
# 将 bkcrab.yaml 的 bkcrab-secrets.STORAGE_DSN 指向它；此文件不是必需的。
#
# 与 bkcrab.yaml 一起使用：
#   1. kubectl apply -f postgres.yml          （创建以下资源）
#   2. 在 bkcrab.yaml 中，设置 bkcrab-secrets.STORAGE_DSN 为：
#        postgres://bkcrab:<POSTGRES_PASSWORD above>@bkcrab-db:5432/bkcrab?sslmode=disable
#   3. kubectl apply -f bkcrab.yaml
apiVersion: v1
kind: Secret
metadata:
  name: bkcrab-db
  namespace: bkcrab
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me-in-production"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bkcrab-db
  namespace: bkcrab
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bkcrab-db
  namespace: bkcrab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bkcrab-db
  template:
    metadata:
      labels:
        app: bkcrab-db
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: bkcrab
        - name: POSTGRES_USER
          value: bkcrab
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: bkcrab-db
              key: POSTGRES_PASSWORD
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "bkcrab"]
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: bkcrab-db
---
apiVersion: v1
kind: Service
metadata:
  name: bkcrab-db
  namespace: bkcrab
spec:
  selector:
    app: bkcrab-db
  ports:
  - port: 5432
