# This is a config for E2B sandbox template.
# You can use template ID (95m23hiiqsx9u65ayuvo) or template name (bkclaw-sandbox) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("bkclaw-sandbox") # Sync sandbox
# sandbox = await AsyncSandbox.create("bkclaw-sandbox") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('bkclaw-sandbox')

start_cmd = "sleep infinity"
dockerfile = "e2b.Dockerfile"
template_name = "bkclaw-sandbox"
template_id = "95m23hiiqsx9u65ayuvo"
