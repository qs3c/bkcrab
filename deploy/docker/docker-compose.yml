name: bkclaw

services:
  bkclaw:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: bkclaw/bkclaw:local
    restart: unless-stopped
    ports:
      - "${BKCLAW_PORT:-18953}:18953"
    volumes:
      - bkclaw-data:/data/.bkclaw
    environment:
      TZ: "${TZ:-Asia/Shanghai}"
      BKCLAW_HOME: /data/.bkclaw
      BKCLAW_BIND: all
      BKCLAW_PORT: "18953"
      BKCLAW_STORAGE_TYPE: postgres
      BKCLAW_STORAGE_DSN: "postgres://bkclaw:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in deploy/docker/.env}@postgres:5432/bkclaw?sslmode=disable"
      BKCLAW_STORAGE_AUTO_MIGRATE: "true"
      BKCLAW_OBJECT_STORE_TYPE: minio
      BKCLAW_OBJECT_STORE_ENDPOINT: minio:9000
      BKCLAW_OBJECT_STORE_BUCKET: "${MINIO_BUCKET:-bkclaw}"
      BKCLAW_OBJECT_STORE_PREFIX: "${MINIO_PREFIX:-}"
      BKCLAW_OBJECT_STORE_ACCESSKEY: "${MINIO_ROOT_USER:?set MINIO_ROOT_USER in deploy/docker/.env}"
      BKCLAW_OBJECT_STORE_SECRETKEY: "${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in deploy/docker/.env}"
      BKCLAW_OBJECT_STORE_USESSL: "false"
      DOCKER_HOST: tcp://sandbox-docker:2375
    depends_on:
      postgres:
        condition: service_healthy
      minio-bootstrap:
        condition: service_completed_successfully
      sandbox-docker:
        condition: service_healthy
    command: ["gateway"]
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:18953/readyz"]
      interval: 5s
      timeout: 3s
      retries: 20

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_DB: bkclaw
      POSTGRES_USER: bkclaw
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in deploy/docker/.env}"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bkclaw -d bkclaw"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:?set MINIO_ROOT_USER in deploy/docker/.env}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in deploy/docker/.env}"
    ports:
      - "${MINIO_API_PORT:-19000}:9000"
      - "${MINIO_CONSOLE_PORT:-19001}:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio-bootstrap:
    image: minio/mc:latest
    restart: "no"
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:?set MINIO_ROOT_USER in deploy/docker/.env}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in deploy/docker/.env}"
      MINIO_BUCKET: "${MINIO_BUCKET:-bkclaw}"
    entrypoint:
      - /bin/sh
      - -ec
      - |
        mc alias set local http://minio:9000 "$${MINIO_ROOT_USER}" "$${MINIO_ROOT_PASSWORD}"
        mc mb --ignore-existing "local/$${MINIO_BUCKET}"

  sandbox-docker:
    image: docker:28-dind
    privileged: true
    restart: unless-stopped
    environment:
      DOCKER_TLS_CERTDIR: ""
    command:
      - --host=tcp://0.0.0.0:2375
      - --host=unix:///var/run/docker.sock
      - --tls=false
    volumes:
      - bkclaw-data:/data/.bkclaw
      - sandbox-docker-data:/var/lib/docker
    healthcheck:
      test: ["CMD", "docker", "info"]
      interval: 5s
      timeout: 3s
      retries: 30

  migrate-storage:
    profiles: ["migration"]
    image: bkclaw/bkclaw:local
    build:
      context: ../..
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./.state:/migration:ro
    entrypoint: ["bkclaw-migrate-storage"]
    command:
      - --sqlite
      - /migration/bkclaw.db
      - --postgres
      - "postgres://bkclaw:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in deploy/docker/.env}@postgres:5432/bkclaw?sslmode=disable"
      - --replace

volumes:
  bkclaw-data:
  postgres-data:
  minio-data:
  sandbox-docker-data:
