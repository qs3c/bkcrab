name: bkclaw

services:
  bkclaw:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: bkclaw/bkclaw:local
    restart: unless-stopped
    ports:
      - "${BKCLAW_PORT:-18953}:18953"
    volumes:
      - bkclaw-data:/data/.bkclaw
    environment:
      TZ: "${TZ:-Asia/Shanghai}"
      BKCLAW_HOME: /data/.bkclaw
      BKCLAW_BIND: all
      BKCLAW_PORT: "18953"
      BKCLAW_STORAGE_TYPE: mysql
      BKCLAW_STORAGE_DSN: "bkclaw:${MYSQL_PASSWORD:?set MYSQL_PASSWORD in deploy/docker/.env}@tcp(mysql:3306)/bkclaw?parseTime=true&loc=UTC&charset=utf8mb4"
      BKCLAW_STORAGE_AUTO_MIGRATE: "true"
      BKCLAW_OBJECT_STORE_TYPE: minio
      BKCLAW_OBJECT_STORE_ENDPOINT: minio:9000
      BKCLAW_OBJECT_STORE_BUCKET: "${MINIO_BUCKET:-bkclaw}"
      BKCLAW_OBJECT_STORE_PREFIX: "${MINIO_PREFIX:-}"
      BKCLAW_OBJECT_STORE_ACCESSKEY: "${MINIO_ROOT_USER:?set MINIO_ROOT_USER in deploy/docker/.env}"
      BKCLAW_OBJECT_STORE_SECRETKEY: "${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in deploy/docker/.env}"
      BKCLAW_OBJECT_STORE_USESSL: "false"
      DOCKER_HOST: tcp://sandbox-docker:2375
    depends_on:
      mysql:
        condition: service_healthy
      minio-bootstrap:
        condition: service_completed_successfully
      sandbox-docker:
        condition: service_healthy
    command: ["gateway"]
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:18953/readyz"]
      interval: 5s
      timeout: 3s
      retries: 20

  mysql:
    image: mysql:8.4
    restart: unless-stopped
    ports:
      - "127.0.0.1:3306:3306"
    environment:
      MYSQL_DATABASE: bkclaw
      MYSQL_USER: bkclaw
      MYSQL_PASSWORD: "${MYSQL_PASSWORD:?set MYSQL_PASSWORD in deploy/docker/.env}"
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:?set MYSQL_ROOT_PASSWORD in deploy/docker/.env}"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u bkclaw -p$${MYSQL_PASSWORD} --silent"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:?set MINIO_ROOT_USER in deploy/docker/.env}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in deploy/docker/.env}"
    ports:
      - "${MINIO_API_PORT:-19000}:9000"
      - "${MINIO_CONSOLE_PORT:-19001}:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio-bootstrap:
    image: minio/mc:latest
    restart: "no"
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:?set MINIO_ROOT_USER in deploy/docker/.env}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD in deploy/docker/.env}"
      MINIO_BUCKET: "${MINIO_BUCKET:-bkclaw}"
    entrypoint:
      - /bin/sh
      - -ec
      - |
        mc alias set local http://minio:9000 "$${MINIO_ROOT_USER}" "$${MINIO_ROOT_PASSWORD}"
        mc mb --ignore-existing "local/$${MINIO_BUCKET}"

  sandbox-docker:
    image: docker:28-dind
    privileged: true
    restart: unless-stopped
    environment:
      DOCKER_TLS_CERTDIR: ""
    command:
      - --host=tcp://0.0.0.0:2375
      - --host=unix:///var/run/docker.sock
      - --tls=false
    volumes:
      - bkclaw-data:/data/.bkclaw
      - sandbox-docker-data:/var/lib/docker
    healthcheck:
      test: ["CMD", "docker", "info"]
      interval: 5s
      timeout: 3s
      retries: 30

  migrate-storage:
    profiles: ["migration"]
    image: bkclaw/bkclaw:local
    build:
      context: ../..
      dockerfile: Dockerfile
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - ./.state:/migration:ro
    entrypoint: ["bkclaw-migrate-storage"]
    command:
      - --sqlite
      - /migration/bkclaw.db
      - --mysql
      - "bkclaw:${MYSQL_PASSWORD:?set MYSQL_PASSWORD in deploy/docker/.env}@tcp(mysql:3306)/bkclaw?parseTime=true&loc=UTC&charset=utf8mb4"
      - --replace

volumes:
  bkclaw-data:
  mysql-data:
  minio-data:
  sandbox-docker-data:
